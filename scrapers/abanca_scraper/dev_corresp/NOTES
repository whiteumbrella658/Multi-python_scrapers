In some cases correspondence's f.emision == mov_f.contable, and it's != mov_f.operacion.
In these cases the scraper WILL NOT FIND the corresponding movement, bcs it searches
where corr_f.emision equal with mov_f.operacion
